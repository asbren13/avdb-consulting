const ConsultingBox = ({ text }) => (
	<div className="box-content">
		<div className="color-box red-bg"></div>
		<div className="title-box heavy">{text}</div>
	</div>
);

export default ConsultingBox;